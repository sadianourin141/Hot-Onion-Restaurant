import React, { useState } from 'react';

const Cart = (foods) => {
    const [counts,setCounts]=useState(0)
    
    
    
    return (
        <div>
            <h2>this is cart</h2>
        </div>
    );
};

export default Cart;